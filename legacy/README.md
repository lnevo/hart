# Legacy / superseded files

Not part of active workflows. Kept for comparison only.

- **`layouts-mac-superseded/`** — older XML (`upper_both3.xml`, `mac_jmri.xml`, `my_layout.xml`, old `tables.xml`)
- **`root-misc/`** — CSV files that lived at the repo root (`group_logic.csv`, I/O lists, etc.)

Active Mac layout artifacts are under [`jmri/layouts/mac/`](../jmri/layouts/mac/).
