# Archived West Yard sheets

These CATS sheet panels and builders are historical. Live Digicon is Master 4 (`HART_Master4.xml` → `wire_hart_master4.py`). Do not launch these as the desk.

`wire_hart_sheet_west_yard2.py` stays in `cats/scripts/` because Master 4 still imports its turnout helpers.
