# Layout host targets — copy to hosts.env and edit

# SSH
HART_PI_SSH=pi
HART_PI_HART=/home/pi/hart
HART_PI_USERFILES=/home/pi/JMRI_UserFiles

HART_WIN_SSH=lnevo@10.0.0.6
HART_WIN_SSH_PORT=2222
HART_WIN_HART=hart

# MQTT (prerequisite — see packages/infra/mqtt-broker/)
HART_MQTT_BROKER=minipc-e5h6x.local
HART_MQTT_PORT=1883

# LCOS Windows COM port
HART_LCOS_COM=COM3
